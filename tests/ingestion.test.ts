import { describe,expect,it } from 'vitest';import { deterministicExtract,partitionByConfidence } from '@/lib/ingestion/service';
describe('ingestion',()=>{it('structures messy project text and gates uncertainty',()=>{const records=deterministicExtract({projects:'1. Phoenix migration - Solution Lead - 14 months - completed\n2. Internal AI accelerator, ongoing',primary:'MS Dynamics, Azure',secondary:'MS Dynamics CRM',tools:'Jira, GitHub'});expect(records.filter(x=>x.type==='project')).toHaveLength(2);expect(records.find(x=>x.type==='project')?.payload.durationMonthsEstimate).toBe(14);const p=partitionByConfidence(records);expect(p.accepted.length).toBeGreaterThan(0);expect(p.needsReview.length).toBeGreaterThan(0)});it('maps aliases to one taxonomy term',()=>{const names=deterministicExtract({primary:'MS Dynamics',secondary:'MS Dynamics CRM'}).map(x=>x.payload.canonicalName);expect(new Set(names)).toEqual(new Set(['Microsoft Dynamics 365']))})});
