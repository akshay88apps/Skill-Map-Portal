export interface SkillGap{leaderEmail:string;skill:string;current:number;target:number}export interface LndConnector{pushGaps(gaps:SkillGap[]):Promise<{batchId:string}>}export class HttpLndConnector implements LndConnector{constructor(private url=process.env.LND_API_URL||'http://localhost:4010'){}async pushGaps(gaps:SkillGap[]){const r=await fetch(`${this.url}/skill-gaps`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({gaps})});if(!r.ok)throw new Error(`L&D adapter: ${r.status}`);return r.json()}}
