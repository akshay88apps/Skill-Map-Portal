import { canonical,splitTerms } from '@/lib/normalization';
export type Extracted={type:'project'|'skill'|'tool';payload:Record<string,unknown>;confidence:number};
export function deterministicExtract(input:{projects?:string;primary?:string;secondary?:string;proficient?:string;tools?:string}):Extracted[]{const result:Extracted[]=[];for(const raw of splitTerms([input.primary,input.secondary,input.proficient].filter(Boolean).join(','))){result.push({type:'skill',payload:{rawText:raw,canonicalName:canonical(raw)},confidence:canonical(raw)===raw?.trim()?.replace(/\b\w/g,c=>c.toUpperCase())?.replace(/[._-]/g,' ')?0.68:0.93})}for(const raw of splitTerms(input.tools))result.push({type:'tool',payload:{name:canonical(raw),rawText:raw},confidence:.9});for(const raw of (input.projects||'').split(/\n(?=\s*(?:[-•\d]|Project))/i).map(x=>x.replace(/^[-•\d.)\s]+/,'').trim()).filter(Boolean)){const duration=raw.match(/(\d+)\s*(months?|years?)/i);const months=duration?Number(duration[1])*(duration[2].toLowerCase().startsWith('year')?12:1):null;result.push({type:'project',payload:{name:raw.split(/[-–|,]/)[0].trim().slice(0,120)||'Untitled project',rawText:raw,durationMonthsEstimate:months,status:/active|ongoing|current/i.test(raw)?'ACTIVE':/closed|complete|ended/i.test(raw)?'CLOSED':'UNKNOWN'},confidence:duration?.[0]&&raw.length>20?.82:.62})}return result}
export function partitionByConfidence(records:Extracted[],threshold=.7){return {accepted:records.filter(x=>x.confidence>=threshold),needsReview:records.filter(x=>x.confidence<threshold)}}
export async function extractWithAI(input:Parameters<typeof deterministicExtract>[0]){if(!process.env.ANTHROPIC_API_KEY)return deterministicExtract(input);/* Production adapter intentionally keeps provider concerns here. */const response=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'content-type':'application/json','x-api-key':process.env.ANTHROPIC_API_KEY,'anthropic-version':'2023-06-01'},body:JSON.stringify({model:'claude-3-5-sonnet-latest',max_tokens:3000,messages:[{role:'user',content:`Return JSON array of structured projects, skills and tools with confidence 0-1. Input: ${JSON.stringify(input)}`} ]})});if(!response.ok)throw new Error(`AI extraction failed: ${response.status}`);const body=await response.json() as any;const text=body.content?.find((x:any)=>x.type==='text')?.text||'[]';return JSON.parse(text.replace(/^```json|```$/g,'').trim()) as Extracted[]}
