import { NextRequest,NextResponse } from 'next/server';import { db } from '@/lib/db';import { isAdmin } from '@/lib/authz';import { reviewInput } from '@/lib/validation';
export async function PATCH(req:NextRequest,{params}:{params:{id:string}}){if(!isAdmin(req))return NextResponse.json({error:'Forbidden'},{status:403});const p=reviewInput.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid action'},{status:422});return NextResponse.json(await db.reviewItem.update({where:{id:params.id},data:{status:p.data.action==='approve'?'APPROVED':'REJECTED',...(p.data.payload?{payload:p.data.payload as any}:{}),reviewedBy:req.headers.get('x-user-email')||'admin',reviewedAt:new Date()}}))}
